import React, { Component } from "react";

export default class Toolbar extends Component {
  render() {
    return (
      <div>
        <button>1</button>
        <button>2</button>
      </div>
    );
  }
}
